/**
 * Represents the different types of tasks (Task, Todo, Deadline, Event).
 */
public enum TaskType {
    TASK, TODO, DEADLINE, EVENT
}

/**
 * Represents the possible status changes for a task (mark, unmark, delete).
 */
enum TaskStatus {
    MARKED, UNMARKED, DELETED
}